# 智能体设计文档
## 小财 · 大学生理财陪伴AI搭子

---

## 一、智能体定义

### 1.1 角色定位

- **产品名**：小财
- **角色定义**：大学生理财全流程的AI陪伴搭子，覆盖攒钱规划、消费管控、入门理财三个场景
- **人设风格**：温柔导师——有耐心、不说教、有金融专业背景，像一个懂理财的学长/学姐
- **口吻示例**：
  - "没关系，很多人第一次都不知道从哪开始，我们一步一步来"
  - "根据你的情况，我建议先从货币基金开始，原因是……"
  - "这个月奶茶花了180，占你餐饮预算的60%，要不要一起看看？"
- **能力边界（硬性限制）**：
  - ✅ 解释金融概念、分析消费结构、提供通用理财建议
  - ✅ 基于用户风险偏好给出方向性建议（如：适合货币基金/指数基金）
  - ❌ 不推荐具体股票代码或基金产品编号
  - ❌ 不承诺任何收益率
  - ❌ 不提供税务/法律建议

### 1.2 核心能力模块

| 模块 | 功能描述 | 触发方式 |
|------|----------|----------|
| **冷启动引导** | 首次使用：理财目标→风险测评→第一步行动建议 | 新用户自动触发 |
| **理财知识问答** | 解答金融概念，自适应用户认知水平 | 用户主动提问 / 快捷入口 |
| **消费录入与复盘** | 手动录入消费，每周主动发起复盘对话 | 录入表单 / 定期主动触发 |
| **情绪陪伴** | 市场波动给认知引导，攒钱遇挫给执行力支持 | 关键词识别 / 用户主动倾诉 |
| **进度追踪** | 追踪攒钱目标进度，展示学习成长 | 快捷入口 / 主动提醒 |

---

## 二、整体技术架构

```
┌──────────────────────────────────────────────────────┐
│                    用户界面层 (Streamlit)              │
│     对话区  |  消费录入表单  |  目标进度卡片            │
└──────────────────────┬───────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────┐
│                   应用逻辑层                           │
│                                                      │
│  ┌─────────────┐    ┌──────────────────────────────┐ │
│  │  场景路由器  │    │       持续学习引擎             │ │
│  │  意图识别   │    │  行为模式提取 / 自适应Prompt  │ │
│  │  分发Handler│    │  用户画像动态更新             │ │
│  └──────┬──────┘    └──────────────────────────────┘ │
│         │                                            │
│  ┌──────▼────────────────────────────────────────┐   │
│  │              对话Handler层                     │   │
│  │  冷启动 | 知识问答 | 消费复盘 | 情绪陪伴       │   │
│  └──────┬────────────────────────────────────────┘   │
│         │                                            │
│  ┌──────▼────────────────────────────────────────┐   │
│  │               工具层 (Tool Use)                │   │
│  │  储蓄计算 | 消费分析 | 目标追踪 | 知识检索    │   │
│  │  复利计算 | 风险评分 | 支出预测               │   │
│  └──────┬─────────────────────────────────────────┘  │
│         │                                            │
│  ┌──────▼────────────────────────────────────────┐   │
│  │               RAG 检索层                       │   │
│  │  查询改写 → ChromaDB检索 → 相关度过滤 → 注入  │   │
│  └───────────────────────────────────────────────┘   │
└──────────────────────┬───────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
┌───────▼──────┐ ┌─────▼──────┐ ┌────▼───────────────┐
│  Claude API  │ │ 四层记忆系统│ │  RAG 知识库        │
│  sonnet-4-6  │ │ 上下文/会话 │ │  ChromaDB          │
│  +Prompt     │ │ 情节/语义   │ │  金融基础知识      │
│  Cache       │ │            │ │  大学生理财场景    │
└──────────────┘ └────────────┘ │  财商教育内容      │
                                └────────────────────┘
```

---

## 三、四层记忆系统设计

小财的记忆系统分为四层，覆盖从当次对话到跨月持久化的全时间维度。

```
┌──────────────────────────────────────────────┐
│  L1 · 上下文记忆（In-Context Memory）         │
│  范围：当前对话窗口最近10-15轮                │
│  内容：当前对话历史                           │
│  存储：Claude API messages参数                │
│  更新：每轮对话                               │
├──────────────────────────────────────────────┤
│  L2 · 会话记忆（Session Memory）              │
│  范围：当次打开App期间                        │
│  内容：用户画像快照、临时交互状态             │
│  存储：Streamlit session_state                │
│  更新：用户操作时                             │
├──────────────────────────────────────────────┤
│  L3 · 情节记忆（Episodic Memory）             │
│  范围：永久持久化，带时间戳                   │
│  内容：消费记录、提问历史、关键事件           │
│  存储：episodes.jsonl（追加写入）             │
│  更新：每次事件发生时                         │
├──────────────────────────────────────────────┤
│  L4 · 语义记忆（Semantic Memory）             │
│  范围：永久持久化，提炼自情节记忆             │
│  内容：风险偏好、学习风格、消费模式、知识图谱 │
│  存储：semantics.json（结构化）               │
│  更新：持续学习引擎定期提炼                   │
└──────────────────────────────────────────────┘
```

### 持久化数据结构

**user_profile.json**（L2/L3 基础画像）
```json
{
  "user_profile": {
    "risk_level": "稳健型",
    "onboarding_done": true,
    "financial_goals": [
      {
        "name": "旅行基金",
        "target_amount": 3000,
        "monthly_saving": 500,
        "deadline": "2026-07",
        "current_amount": 1000
      }
    ],
    "learned_concepts": ["货币基金", "风险分散", "复利"]
  }
}
```

**episodes.jsonl**（L3 情节记忆，追加写入）
```jsonl
{"timestamp":"2026-05-10T14:23:00","type":"expense","content":{"amount":45,"category":"餐饮","note":"午饭"}}
{"timestamp":"2026-05-10T15:01:00","type":"question_asked","content":{"text":"基金和股票有什么区别？"}}
{"timestamp":"2026-05-11T09:00:00","type":"risk_test_completed","content":{"result":"稳健型","score":6}}
```

**semantics.json**（L4 语义记忆，持续更新）
```json
{
  "inferred_risk_profile": {"profile": "conservative", "confidence": 0.82},
  "learning_style": "concise",
  "avg_weekly_spending": 320,
  "top_spending_category": "餐饮",
  "concept_maturity": {
    "货币基金": 0.9,
    "指数基金": 0.4,
    "股票": 0.2
  },
  "engagement_score": 0.75
}
```

### 上下文治理策略

对话历史超过阈值时，自动压缩旧对话：
1. **滑动窗口**：保留最近15轮完整对话（日常主策略）
2. **摘要压缩**：超过20轮时，用 claude-haiku-4-5 将旧对话摘要为200字，插入对话首部
3. **关键信息固定**：用户画像、风险偏好等关键事实永远保留在 system prompt 中，不被压缩

---

## 四、持续学习与自我进化

小财通过分析用户行为持续更新自身对用户的理解，无需微调模型。

### 4.1 行为信号提取

| 信号来源 | 提取逻辑 | 更新内容 |
|----------|----------|----------|
| 用户问题中的关键词 | "安全/保本" → 保守；"高收益/赚钱" → 积极 | 动态修正风险偏好 |
| 回复长度偏好 | 用户追问"能详细说吗" vs 从不追问 | 更新 learning_style |
| 消费录入频率 | 每天录入 vs 偶尔录入 | 调整主动提醒频率 |
| 概念理解确认 | 用户说"懂了/明白了" | 将知识点加入 learned_concepts |
| 情绪词触发 | "跌了/好慌/要不要卖" | 触发情绪陪伴场景 |

### 4.2 自适应 System Prompt

每次对话前，持续学习引擎动态生成个性化指令，追加到 system prompt：

```
### 动态自适应规则（由学习引擎生成）
- 用户学习风格：简洁型，请用bullet points，回复不超过150字
- 用户风险偏好：稳健型（行为推断置信度82%），给建议时优先推荐低波动产品
- 已掌握概念：货币基金、复利，请跳过基础介绍直接进阶
- 消费特征：餐饮支出偏高，复盘时重点关注餐饮类别
```

### 4.3 进化路径示意

```
第1次对话  →  基础风险测评 + 理财目标收集
第3次对话  →  根据提问关键词修正风险偏好推断
第7次对话  →  检测到用户偏好简洁回答，切换输出风格
第15次对话 →  concept_maturity > 0.8，自动推送进阶知识
第30次对话 →  消费模式稳定，主动提供个性化攒钱建议
```

---

## 五、工具层设计（Tool Use）

Claude 通过 tool_use API 调用以下工具，确保数值计算准确、知识检索稳定。

### 5.1 工具清单

| 工具名 | 功能 | 输入参数 | 典型触发场景 |
|--------|------|----------|-------------|
| `calculate_monthly_savings` | 计算每月可存金额 | 收入、支出、储蓄率目标 | "我每月能存多少钱？" |
| `calculate_investment_timeline` | 计算达成目标所需时间 | 当前存款、月投入、目标金额、预期收益率 | "我要攒3000元需要多久？" |
| `calculate_compound_interest` | 复利计算与可视化 | 本金、月投入、年利率、年限 | "坚持定投10年能有多少钱？" |
| `analyze_expense_distribution` | 消费结构分析 | 近期消费记录列表 | 周复盘 / 月复盘触发 |
| `forecast_month_end_spending` | 预测月末支出 | 当月已有消费、当前日期 | "我这个月还能花多少？" |
| `get_financial_concept` | 检索金融知识 | 概念名、难度等级 | 知识问答场景 |
| `score_risk_tolerance` | 风险偏好评分 | 三题测评答案 | 冷启动风险测评 |

### 5.2 工具使用流程

```
用户："我每月生活费1500，想攒旅行基金3000元，要多久？"

Claude 识别需要计算 →
  调用 calculate_investment_timeline(
    current_savings=0,
    monthly_investment=500,
    target_amount=3000,
    annual_return_rate=0.02
  )

工具返回：{"months_to_goal": 6, "advice": "按每月存500元，约6个月可达成"}

Claude 基于工具结果生成回答：
  "按你的情况，每月存500元放货币基金（约2%年化），
   大概6个月就能攒到3000元的旅行基金～
   要不要现在就设一个提醒，每月固定转？"
```

---

## 六、对话流设计

### 6.1 冷启动流程（新用户）

```
[开场白]
小财："你好！我是小财，你的理财搭子～
      先问你：你现在最想用钱实现什么？
      比如攒旅游基金、备考研备用金，或者就是想让钱别那么快花完？"

[步骤1：理财目标]
用户说出目标 → 小财确认金额和时间
→ 调用 calculate_investment_timeline，告诉用户每月需存多少

[步骤2：风险测评，3题对话式]
Q1："存的1000元第二天变成950元，你的第一反应？"
Q2："这笔钱多久之内要用到？"
Q3："你对理财的期望是？"
→ 调用 score_risk_tolerance，判定 保守/稳健/积极

[步骤3：第一步行动建议]
稳健型示例：
"根据你的情况，建议这样开始：
 ① 每月先存500元进货币基金（余额宝类），随时可取
 ② 攒到1000元后，我再教你指数基金定投 ✨
 这是你的第一步，简单但很重要。"

[保存画像 → 进入主界面]
```

### 6.2 日常对话主界面

```
┌────────────────────────────────────────┐
│  🌱 小财  你的理财搭子          [设置]  │
├────────────────────────────────────────┤
│  🎯 旅行基金  ▓▓▓░░░  1000/3000元      │
├────────────────────────────────────────┤
│  [对话区]                               │
│                                        │
│  小财：上周你录入了5笔消费，            │
│       奶茶占餐饮的60%，聊聊？           │
│                                        │
│  用户：好啊～                           │
│                                        │
│  [消费分析卡片 by analyze_expense工具]  │
│                                        │
├────────────────────────────────────────┤
│  💬 输入框              [📊 录入消费]   │
│  [📚 学知识] [🎯 我的目标] [💡 今日问] │
└────────────────────────────────────────┘
```

### 6.3 情绪陪伴流程

```
触发词：亏了/跌了/要不要卖/好慌/割肉/后悔

小财响应三步骤：
  ① 先共情："跌了确实很难受，这种感觉很正常"
  ② 引导认知："大多数人亏钱不是因为行情差，
                而是在最低点割肉——你知道为什么吗？"
  ③ 给决策框架："判断要不要卖，可以问自己三个问题：
                  1. 当初买的理由还成立吗？
                  2. 你的钱近期需要用吗？
                  3. 你能承受继续跌10%吗？"

注意：不给"卖" or "不卖"的直接答案
```

### 6.4 消费录入与复盘

```
[录入表单]
金额 | 类别（餐饮/娱乐/学习/生活/其他）| 日期 | 备注（选填）

[录入后即时反馈]
→ 调用 forecast_month_end_spending
"奶茶+35元，本月餐饮已花180，
 按这个节奏月底餐饮预计花420元，比上月多30%～"

[每周主动复盘]
→ 调用 analyze_expense_distribution
"上周录入5笔，共花320元。
 餐饮180（56%）、娱乐80（25%）、学习60（19%）。
 娱乐支出比上周多了40%，两笔奶茶+一场电影，
 这个算在计划内吗？"
```

---

## 七、系统 Prompt 结构

```
[1. 静态角色定义]          ← cache_control: ephemeral（Prompt Caching）
你是小财，大学生理财AI搭子。温柔导师风格。能力边界：...

[2. 动态用户画像]          ← 每次对话重新注入，不缓存
用户风险偏好：稳健型
理财目标：旅行基金3000元（已攒1000元）
已掌握知识：货币基金、复利
本月消费情况：已录入8笔，餐饮占比最高

[3. 持续学习引擎生成的自适应规则]
用户偏好简洁回答，请用bullet points，不超过150字
用户近期有情绪波动信号，注意共情...

[4. 当前场景指令]          ← 由场景路由器注入
当前场景：消费复盘
请基于以下数据发起复盘对话：[消费数据]
```

---

## 八、文件结构

```
ZSYH-PM-competiton/
├── app.py                      # Streamlit主程序入口
├── agent/
│   ├── router.py               # 意图识别与场景路由
│   ├── handlers.py             # 各场景对话Handler
│   ├── tools.py                # 工具层（Tool Use注册）
│   ├── memory.py               # 四层记忆读写
│   ├── learning.py             # 持续学习引擎
│   └── context_manager.py     # 上下文治理（压缩/摘要）
├── prompts/
│   ├── system_base.md          # 静态角色定义（走Prompt Cache）
│   ├── onboarding.md           # 冷启动场景指令
│   ├── knowledge_qa.md         # 知识问答场景指令
│   ├── expense_review.md       # 消费复盘场景指令
│   └── emotion_support.md      # 情绪陪伴场景指令
├── data/
│   ├── user_profile.json       # 基础画像（L2/L3）
│   ├── episodes.jsonl          # 情节记忆（L3，追加写）
│   └── semantics.json          # 语义记忆（L4，持续更新）
├── requirements.txt
└── .streamlit/
    └── config.toml             # UI主题
```

---

## 九、RAG 知识库系统

### 9.1 为什么需要 RAG

小财是垂类 Agent，纯靠大模型有两个问题：
1. **金融知识可能"编造"数据**——利率、产品规则等数值不准确
2. **大学生场景知识通用模型覆盖不足**——校园消费场景、学生理财习惯等细节需要专属知识

RAG 让小财的回答有据可查，同时保证垂类场景的专业深度。

### 9.2 知识库三类内容

| 类别 | 内容范围 | 文档数量（目标） | 优先级 |
|------|----------|----------------|--------|
| **金融基础知识** | 货币基金/基金/股票/ETF/债券/复利/风险/资产配置/定投等概念解释 | 20篇 | P0 |
| **大学生理财场景** | 校园消费心理、生活费管理、兼职收入处理、攒钱方法论、应急金建立 | 15篇 | P1 |
| **财商教育内容** | 消费心理学、行为金融学、记账方法、理财习惯养成、常见误区 | 15篇 | P1 |

### 9.3 RAG 工作流程

```
用户提问："基金定投和一次性买入哪个好？"
         ↓
[查询改写]
  将用户口语化问题改写为检索友好的关键词：
  "定投 一次性买入 比较 优缺点"
         ↓
[ChromaDB 语义检索]
  从知识库中检索 Top-3 相关文档片段
  相似度阈值：> 0.7 才采用
         ↓
[相关度过滤]
  检查检索结果与用户问题的相关性
  若无相关结果（< 0.7）：不注入，让模型自行回答
         ↓
[上下文注入]
  将检索到的文档片段注入 system prompt：
  "以下是相关知识库内容，请基于此回答用户问题：
   [文档片段1]
   [文档片段2]"
         ↓
[Claude 生成回答]
  基于检索内容 + 用户画像（风险偏好/学习水平）生成个性化回答
```

### 9.4 知识文档格式规范

每个知识文档统一格式，便于 ChromaDB 建索引：

```json
{
  "doc_id": "finance_001",
  "category": "金融基础知识",
  "title": "货币基金是什么",
  "difficulty": "入门",
  "tags": ["货币基金", "低风险", "流动性", "余额宝"],
  "content": "货币基金是一种主要投资于短期债务工具的基金...",
  "summary": "低风险、高流动性的入门理财工具，适合存放备用金",
  "suitable_for": ["保守型", "稳健型"],
  "updated_at": "2026-05-15"
}
```

### 9.5 文件结构

```
ZSYH-PM-competiton/
├── rag/
│   ├── build_index.py          # 一次性脚本：读取文档库，建ChromaDB索引
│   ├── retriever.py            # RAG检索模块（查询改写+检索+过滤）
│   ├── knowledge_base/
│   │   ├── finance_basics/     # 金融基础知识（20篇 .json）
│   │   ├── student_scenarios/  # 大学生理财场景（15篇 .json）
│   │   └── financial_literacy/ # 财商教育内容（15篇 .json）
│   └── chroma_db/              # ChromaDB 持久化目录（.gitignore）
```

### 9.6 与 Skill 系统的协作

RAG 和 Skill 不是并列关系，而是有明确分工：

| 场景 | 使用 Skill | 使用 RAG |
|------|-----------|---------|
| 情绪陪伴 | ✅ emotion_support Skill 主导 | ❌ 不需要 |
| 金融概念解释 | ✅ explain_financial_concept Skill 控制结构 | ✅ RAG 提供准确知识内容 |
| 消费复盘 | ✅ expense_review Skill 主导 | ❌ 数据来自用户记录 |
| 数值计算 | ✅ Function Skill（Tool Use） | ❌ 不需要 |
| 行为金融/财商 | ✅ Skill 控制输出格式 | ✅ RAG 提供专业内容支撑 |

---

## 十、Skill 自进化系统

这是小财"越用越智能"的核心机制。每次对话后，agent 自动将经验封装为 Skill；每次使用 Skill 时自我观察并优化；用户每次反馈都是对 Skill 的一次训练。

### 9.1 Skill 的存在形式

小财的 Skill 分两类，混合使用：

| Skill 类型 | 适用场景 | 存储形式 | 进化方式 |
|------------|----------|----------|----------|
| **Prompt Skill** | 知识解释、情绪回应、建议生成等自然语言任务 | Prompt 模板 + 参数字典 | 优化模板措辞和参数默认值 |
| **Function Skill** | 储蓄计算、消费分析等数值计算任务 | Python 函数 + 元数据 | 优化参数逻辑和输出格式 |

每个 Skill 的数据结构：

```json
{
  "skill_id": "explain_fund_basics",
  "type": "prompt",
  "name": "解释基金基础概念",
  "version": 3,
  "created_at": "2026-05-10T10:00:00",
  "last_updated": "2026-05-12T15:30:00",
  "use_count": 24,
  "positive_feedback": 19,
  "negative_feedback": 2,
  "satisfaction_rate": 0.905,
  "template": "请用{style}的方式向一个{user_level}的大学生解释{concept}。要求：{constraints}",
  "default_params": {
    "style": "类比+举例",
    "constraints": "不超过150字，避免专业术语，最后问一句确认理解"
  },
  "optimization_log": [
    {"version": 1, "change": "初始版本", "satisfaction_before": null},
    {"version": 2, "change": "加入'最后问一句确认理解'约束，用户追问减少40%", "satisfaction_before": 0.72},
    {"version": 3, "change": "将style默认值从'直接解释'改为'类比+举例'", "satisfaction_before": 0.85}
  ]
}
```

### 9.2 Skill 的三种生命周期阶段

```
[阶段一：封装]          [阶段二：观察]          [阶段三：进化]
对话/任务完成后  →    每次使用时自我审查   →   累积反馈后触发优化
自动提取模式         是否步骤多余？              Prompt 参数更新
封装为新 Skill       参数是否可优化？            函数逻辑调整
                     用户是否修改输出？          版本号+1，记录变更日志
```

### 9.3 阶段一：自动封装 Skill

每次对话轮次结束后，Agent 内省提取可复用模式：

```
触发条件：
  - 对话轮次 >= 3（有足够上下文）
  - 用户表达满意（"懂了/好的/明白了"等确认词）
  - 或任务完成（风险测评完成、消费复盘结束）

封装流程：
  Step 1: Agent 用 meta-prompt 分析本轮对话
          "这次对话中，我完成了什么任务？用了什么方法？有哪些步骤是可复用的？"

  Step 2: 生成 Skill 草稿
          - 识别任务类型（解释/计算/建议/陪伴）
          - 提取核心 Prompt 模板或函数逻辑
          - 记录使用的参数和上下文条件

  Step 3: 检查去重
          - 与已有 Skill 库比较相似度
          - 相似度 > 0.85：合并更新已有 Skill
          - 相似度 < 0.85：创建新 Skill

  Step 4: 写入 skills/ 目录
```

### 9.4 阶段二：使用时自我观察

每次 Agent 调用某个 Skill 时，同步记录观察日志：

```python
class SkillObserver:
    """Agent 执行 Skill 时的自我观察机制"""

    def observe_execution(self, skill_id, input_params, output, user_response):
        observations = {
            "redundant_steps": self._detect_redundancy(output),
            # 输出中有无用户跳过/忽略的部分？
            
            "param_deviation": self._detect_param_change(skill_id, input_params),
            # Agent 是否临时修改了默认参数？为什么？
            
            "output_modified": self._detect_user_edit(output, user_response),
            # 用户是否复述或纠正了输出的某个部分？
            
            "followup_confusion": self._detect_confusion(user_response),
            # 用户下一句是否是困惑性追问？（说明输出不够清晰）
        }
        self._save_observation(skill_id, observations)
```

**四个观察维度：**

| 观察维度 | 检测方式 | 优化方向 |
|----------|----------|----------|
| 步骤是否多余 | 输出某段内容后用户立即跳过/无响应 | 精简模板，去除冗余步骤 |
| 参数是否偏离 | Agent 执行时偏离了默认参数 | 将偏离值更新为新默认值 |
| 用户是否修改输出 | 用户复述时纠正了某个数字或措辞 | 调整计算逻辑或表达方式 |
| 是否引发困惑追问 | 下一句含"什么意思/能再解释吗" | 降低解释难度，增加例子 |

### 9.5 阶段三：反馈驱动进化

用户点击 👍/👎 后触发 Skill 更新流程：

```
[用户点击 👍]
  → 记录正向反馈到 Skill 元数据
  → satisfaction_rate 更新
  → 若 use_count >= 5 且 satisfaction_rate > 0.85：
      将当前参数设为"稳定版本"，降低自动优化频率

[用户点击 👎（可附文字说明）]
  → 记录负向反馈 + 用户原话（若有）
  → 触发 meta-reflection：
      Agent 用以下 Prompt 自我分析：
      "这次回答用了 {skill_name}。用户给了差评，说：'{feedback}'。
       请分析：是模板问题、参数问题还是上下文理解问题？
       建议如何修改模板或参数？"
  → 生成优化建议 → 写入 optimization_log
  → version + 1，更新 Skill 文件

[自动批量优化（每10次使用触发一次）]
  → 分析最近10次的观察日志
  → 识别高频问题模式
  → 生成综合优化方案并执行
```

### 9.6 Skill 库文件结构

```
ZSYH-PM-competiton/
├── skills/
│   ├── registry.json                    # Skill 索引（id/name/version/satisfaction）
│   ├── prompt_skills/
│   │   ├── explain_fund_basics.json     # 解释基金基础
│   │   ├── explain_risk_concept.json    # 解释风险概念
│   │   ├── emotion_support.json         # 情绪陪伴
│   │   ├── onboarding_guide.json        # 冷启动引导
│   │   └── savings_advice.json          # 储蓄建议
│   └── function_skills/
│       ├── calculate_savings.json       # 储蓄计算（元数据）
│       ├── analyze_expense.json         # 消费分析（元数据）
│       └── compound_interest.json       # 复利计算（元数据）
└── skill_observations/
    └── {skill_id}_{date}.jsonl          # 每次使用的观察日志
```

### 9.7 进化效果可视化（演示用）

在 Agent 界面提供"Skill 成长记录"展示，作为产品亮点：

```
📈 小财的成长记录

  explain_fund_basics  v1→v3  满意度 72%→91%  使用24次
  ├─ v2 优化：加入确认理解的问句，用户追问减少40%
  └─ v3 优化：改用类比举例风格，满意度再提升6%

  emotion_support      v1→v2  满意度 68%→88%  使用11次
  └─ v2 优化：删除"你应该..."句式，改为提问引导

  analyze_expense      v1→v1  满意度 93%       使用18次
  └─ 稳定，暂无优化
```

---

## 十、评测设计（预留）

评测维度与测试用例详见 `evaluation.md`，在Agent实现完成后执行。

| 评测维度 | 评测方法 |
|----------|----------|
| 金融知识准确性 | 10道标准金融题，人工验证答案 |
| 人设一致性（温柔导师） | 5个压力场景，检验是否说教/承诺收益 |
| 能力边界把控 | 故意问"推荐某只股票"，验证是否拒绝 |
| 情绪陪伴质量 | 3个大跌场景，验证是否给认知引导而非操作建议 |
| 个性化记忆 | 第二次登录，验证是否记住风险偏好/已学知识点 |
| 工具调用准确性 | 验证计算工具的数值结果是否正确 |
