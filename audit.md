# 小财功能审计报告
## 已实现 vs 设计差距 vs 超出设计的缺失

---

## 一、已实现功能 ✅

| 功能 | 实现位置 | 完整度 |
|------|----------|--------|
| 冷启动三步流程（目标→测评→建议） | app.py 结构化状态机 | ✅ 完整 |
| 场景路由（6种意图） | agent/router.py | ✅ 完整 |
| 四层记忆系统（L1-L4） | agent/memory.py | ✅ 框架完整 |
| 工具层 Tool Use（5个工具） | agent/tools.py | ✅ 完整 |
| RAG检索（金融基础20篇） | rag/retriever.py | ✅ 基础可用 |
| 自适应 System Prompt | agent/learning.py | ✅ 基础可用 |
| Skill系统框架 | agent/skills_manager.py | ✅ MVP可用 |
| 反馈触发 meta-reflection | agent/skills_manager.py | ✅ 已实现 |
| 消费录入+即时反馈+图表 | app.py 侧边栏 | ✅ 完整 |
| 目标进度可视化 | app.py render_goal_card | ✅ 完整 |
| 情绪陪伴（三步骤） | agent/handlers.py | ✅ 完整 |
| <think>标签过滤 | agent/handlers.py | ✅ 完整 |
| 👍👎 反馈按钮 | app.py | ✅ 完整 |
| Skill成长展示面板 | app.py 侧边栏 | ✅ 完整 |
| 快捷入口（4个） | app.py 侧边栏 | ✅ 完整 |

---

## 二、design.md 中有但未完整实现的功能 ❌

### B1. 语义记忆进化路径未实现
**设计**：第3/7/15/30次对话自动触发不同程度的自适应（design.md 进化路径示意）
**现状**：无对话轮次计数器，无基于轮次的触发逻辑
**影响**：Agent "越用越懂用户"的核心体验无法实现

### B2. Skill自动封装未实现
**设计**：每次对话后自动提取可复用模式，封装为新Skill
**现状**：只有手动预设的5个Skill，无自动生成
**影响**：Skill库无法自我扩展

### B3. 上下文摘要压缩未实现
**设计**：超过20轮时用轻量模型将旧对话压缩为200字摘要
**现状**：只有滑动窗口（最近15条），老对话直接丢弃
**影响**：长对话时遗忘早期重要信息（如目标设定场景）

### B4. 情绪识别词汇不完整
**设计**：覆盖市场波动焦虑 + 攒钱受挫两类场景
**现状**：只有市场词汇，"坚持不下去/又花完了"等攒钱受挫词汇缺失
**影响**：漏掉约30%的真实情绪信号

### B5. 主动复盘触发完全未实现
**设计**：每周一主动发起消费复盘
**现状**：0行代码，纯被动等待
**影响**：用户粘性低，无陪伴感

### B6. RAG知识库不完整
**设计**：50篇（20金融基础+15大学生场景+15财商教育）
**现状**：student_scenarios和financial_literacy目录已有文件但索引未包含
**待确认**：需重新运行build_index.py

---

## 三、超出design.md设计、Agent产品角度的重大缺失 🔍

### C1. 多目标管理缺失（高优先级）
**问题**：profile["financial_goals"]支持多目标，但界面只展示goals[0]
**修复**：render_goal_card展示所有目标，对话中支持指定更新哪个目标

### C2. 能力边界动态检查缺失（高优先级）
**问题**：system_base.md里有静态声明，但LLM输出没有后置检查
**建议**：在_call_llm返回后做关键词扫描（推荐具体股票/承诺收益率），发现违规则提示

### C3. 错误降级处理缺失（中优先级）
**问题**：LLM超时、RAG挂掉、tools报错均无graceful degradation
**建议**：_call_llm加try/except+重试，retrieve()失败时返回空不crash

### C4. 学习路径推荐缺失（中优先级）
**问题**：用户学了"货币基金"后，Agent不会主动推荐"下一步学指数基金"
**建议**：在generate_adaptive_prompt_addon里加入"推荐下一个概念"逻辑

### C5. 消费长期趋势分析缺失（中优先级）
**问题**：只能查看最近30天，无法跨月对比消费变化
**建议**：EpisodicMemory.get_recent()支持自定义时间范围参数

### C6. 对话轮次计数器缺失（中优先级）
**问题**：进化路径依赖轮次，但无计数机制
**修复**：在profile里加interaction_count字段，每次dispatch后+1

### C7. 前后矛盾风险（低优先级）
**问题**：行为推断的风险偏好（置信度65%）可能覆盖用户明确测评结果
**建议**：行为推断结果只作为"补充说明"，不覆盖测评结果

### C8. 冷启动目标可行性验证缺失（低优先级）
**问题**：用户设定月存1200元但月收入1500元时，没有提醒
**建议**：calculate_investment_timeline后加合理性检查（月存>月收入50%则警告）

---

## 四、新增功能建议（设计文档未提及）

### N1. 对话搜索/历史回顾
用户说"我之前问过基金是什么，能帮我找到吗"——当前无法做

### N2. 财务健康评分
基于消费记录、储蓄进度、风险偏好，给用户一个月度"财务健康分"，可视化展示

### N3. 理财概念思维导图
用户学过的概念自动组织成知识图谱，展示概念间的关联关系

### N4. 模拟投资计算器
"如果我3年前开始定投沪深300，现在有多少钱"——反事实计算，增加学习动机

### N5. 账单智能解读
用户粘贴支付宝/微信账单截图或文字，Agent自动分类和分析（需要OCR或结构化输入）

---

## 五、修复优先级建议

| 优先级 | 功能 | 预估工时 | 演示价值 |
|--------|------|----------|----------|
| P0 | B4 情绪词汇补全 | 10min | 高 |
| P0 | C3 错误降级处理 | 30min | 高（防crash） |
| P0 | C1 多目标展示 | 20min | 中 |
| P1 | C6 对话轮次计数 | 15min | 高（支撑进化路径） |
| P1 | C4 学习路径推荐 | 30min | 高（差异化亮点） |
| P1 | B6 RAG索引完整性确认 | 5min | 中 |
| P2 | B3 上下文摘要压缩 | 1h | 中 |
| P2 | C2 能力边界检查 | 30min | 中 |
| P3 | B1/B2 进化路径/Skill自动封装 | 3h+ | 低（演示看不出来） |
| P3 | N2 财务健康评分 | 2h | 高（亮点功能） |
